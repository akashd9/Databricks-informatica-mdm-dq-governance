project_tag: mdm-dq-medallion
lookback_days: 1
daily_budget_usd: 25.0
hard_limit_usd: 100.0

# Rough list-price estimate per DBU by SKU — NOT billing-accurate (actual
# price depends on cloud, region, and contract/discount terms). Good enough
# for "is this trending somewhere unexpected", not for finance reporting.
dbu_price_usd:
  STANDARD_ALL_PURPOSE_COMPUTE: 0.40
  STANDARD_JOBS_COMPUTE: 0.15
  PREMIUM_JOBS_COMPUTE: 0.30
  default: 0.25
